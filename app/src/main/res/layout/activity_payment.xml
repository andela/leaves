<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/payment_option_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.worldtreeinc.leaves.activity.PaymentOptionActivity">

    <TextView
        android:id="@+id/payment_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="10dp"
        android:textStyle="bold"
        android:text="@string/title_activity_event_details"
        android:textSize="20sp"/>

    <TextView
        android:id="@+id/payment_name_title"
        style="@style/payment_activity_text_title_style"
        android:text="@string/payment_name_title"/>

    <TextView
        android:id="@+id/payment_name"
        style="@style/payment_activity_text_style"/>

    <TextView
        android:id="@+id/payment_amount_title"
        style="@style/payment_activity_text_title_style"
        android:text="@string/payment_amount_title"/>

    <TextView
        android:id="@+id/payment_amount"
        style="@style/payment_activity_text_style"/>

    <TextView
        android:id="@+id/amount"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="10dp"
        android:textSize="16sp"/>

    <Button
        android:id="@+id/paypal_payment_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/paypal_button_string" />

</LinearLayout>
